# 小说 → 短剧视频 生产方案（H3 固定方案）

> 版本：v1.6 ｜日期：2026-09-01
> 适用范围：个人单机生产，ComfyUI + MiniMax H3 主线

---

## 目录

- [〇、方案定稿声明](#〇方案定稿声明)
- [一、主线流程总览](#一主线流程总览)
- [二、硬件环境](#二硬件环境)
- [三、软件清单](#三软件清单)
- [四、ComfyUI](#四comfyui)
- [五、模型文件与目录结构（约 43GB）](#五模型文件与目录结构约-43gb)
- [六、插件](#六插件)
- [七、SageAttention](#七sageattention)
- [八、工作流演进路线（按阶段推进）](#八工作流演进路线按阶段推进)
- [九、H3 Multishot（连续镜头）](#九h3-multishot连续镜头)
- [十、连续剧分镜生产法](#十连续剧分镜生产法)
- [十一、H3 对白写作规范](#十一h3-对白写作规范)
- [十二、素材与产物保存规范（output 目录与命名）](#十二素材与产物保存规范output-目录与命名)
- [十三、角色一致性](#十三角色一致性)
- [十四、场景一致性](#十四场景一致性)
- [十五、Codex / CodeBuddy 编剧与分镜（无需开发）](#十五codex--codebuddy-编剧与分镜无需开发)
- [十六、剪辑（剪映）](#十六剪辑剪映)
- [十七、Turbo LoRA（最后阶段）](#十七turbo-lora最后阶段)
- [十八、关于 20 分钟长视频（概念纠正）](#十八关于-20-分钟长视频概念纠正)
- [十九、硬件路线（A5000 24GB 分阶段）](#十九硬件路线a5000-24gb-分阶段)
- [二十、执行顺序（10 步）](#二十执行顺序10-步)
- [二十一、关键要点速记](#二十一关键要点速记)
- [附录 A：多集 / 整剧生产要点](#附录-a多集--整剧生产要点)
- [附录 B：常见问题排查（FAQ）](#附录-b常见问题排查faq)

---

## 〇、方案定稿声明

本方案基于 ComfyUI 原生 MiniMax H3 支持 + 社区验证的 H3 Fast 工作流整理。

**除非 H3 / ComfyUI 本身发生重大变化，否则不再更换整套架构。**

一句话主线：

> Codex/CodeBuddy 做编剧和分镜 → ComfyUI 做角色/场景/首尾帧 → MiniMax H3 做带声音的视频 → H3 Multishot 做连续镜头 → 剪映成片

---

## 一、主线流程总览

```
                    小说
                     │
                     ▼
           Codex / CodeBuddy
                     │
                     ▼
            ┌────────────────┐
            │ 剧本 + 分镜表   │
            │ 人物设定       │
            │ 场景设定       │
            │ H3 Prompt      │
            └───────┬────────┘
                    │
                    ▼
               ┌─────────┐
               │ ComfyUI │
               └────┬────┘
                    │
       ┌────────────┼─────────────┐
       ▼            ▼             ▼
    人物参考       场景参考       首帧/尾帧
 (三视图+全景) (三视图+全景)         │
       │            │             │
       └────────────┼─────────────┘
                    ▼
              MiniMax H3
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
        视频                 原生音频
          │                   │
          └─────────┬─────────┘
                    ▼
              H3 Multishot
                    │
                    ▼
                  剪映
                    │
                    ▼
                 最终短剧
```

---

## 二、硬件环境

| 组件 | 配置 |
|---|---|
| CPU | R9 9950X3D |
| 内存 | 96GB RAM |
| GPU | RTX A5000 24GB（Ampere 架构） |

**24GB 显存定位**：第一阶段必须走「INT8 + Pruned + SageAttention」的低显存路线，不要一上来堆完整模型 + 大编码器 + REF2VA + 高分辨率。

---

## 三、软件清单

### 必须安装

| 软件 | 用途 |
|---|---|
| ComfyUI（≥ 0.30.0） | 图片 / 视频生成 |
| Codex / CodeBuddy | 编剧 + 分镜（产出 10 项分镜表） |
| MiniMax H3 | 视频 + 原生音频 |
| KJNodes | H3 辅助节点 / SageAttention / 图片尺寸处理 |
| rgthree-comfy | LoRA 管理 |
| SageAttention 2.x | 注意力加速（内存 + 速度） |
| H3 Multishot | 连续镜头串联（[GitHub](https://github.com/jlucasmcrell/ComfyUI-H3-Multishot)） |
| 剪映 | 最终剪辑 |

### 暂时不要

| 软件 | 原因 |
|---|---|
| Dify | 不需要 |
| LangChain | 不需要 |
| 自己写 Agent | 不需要（若想自动化可先用现成插件 [MiniMax-H3-Codex-Drama](https://github.com/chiphoton/MiniMax-H3-Codex-Drama)，第四阶段后再考虑） |
| short-drama-agent | 不适用：Codex Skill + 火山方舟 Seedream/Seedance 云端 API，与本地 ComfyUI + H3 固定架构不符；其「连续性状态机 / 资产验收 / 审片只重跑失败镜头」方法论可参考，无需安装 |
| 数据库 | 不需要 |
| CosyVoice | H3 原生声音先够用 |
| FFmpeg | 暂时不需要（若用 Codex-Drama 插件做自动剪辑/QC 则需安装） |
| GPT-SoVITS | 暂时不需要 |

### 最终下载清单（必装 4 项 + 可选 1 项）

> 全流程真正需要下载的只有这些，不用再装十几个项目。

| # | 组件 | 仓库地址 | 说明 |
|---|---|---|---|
| ① | ComfyUI | <https://github.com/Comfy-Org/ComfyUI> | 官方仓库（原 comfyanonymous 已迁移至 Comfy-Org），GPL-3.0；需 **≥ 0.30.0** |
| ② | MiniMax H3（ComfyUI 支持） | <https://github.com/MiniMax-AI/MiniMax-H3> | **MiniMax 官方仓库**，提供 ComfyUI 教程（T2V / R2V 模板）；模型托管在 Hugging Face `MiniMaxAI/MiniMax-H3` |
| ③ | 官方 ComfyUI 模板 | <https://github.com/Comfy-Org/workflow_templates> | 官方模板仓库，**确认包含 H3 系列模板**（media-assets-01 分类）：`video_minimax_h3_t2v` / `i2v` / `r2v` / `i2v_continuation`（本地 ComfyUI 用 `video_` 前缀这几个；`api_` 前缀是云端 API 调用模板，本地跑用不上）；MiniMax 官方教程模板 + Multishot 附带工作流仍可作为对照参考 |
| ④ | H3 Multishot | <https://github.com/jlucasmcrell/ComfyUI-H3-Multishot> | 连续镜头串联（详见第九章） |
| ⑤ | H3 Turbo（**可选**） | <https://github.com/Larryvrh/ComfyUI-MiniMax-H3-Turbo> | 最后阶段再装（详见第十七章） |

> 模型文件目录结构见第五章；插件（KJNodes / rgthree-comfy / SageAttention）见第六章。

---

## 四、ComfyUI

- 版本要求：**≥ 0.30.0**
- 原因：H3 Multishot 明确要求 0.30.0+，该版本已具备**原生 MiniMax H3 支持**。
- 安装方式：官方 Desktop 版 / 便携包 / `git clone` 均可；装好后建议装 **ComfyUI-Manager**，后续插件搜索安装、版本更新都靠它。

---

## 五、模型文件与目录结构（约 43GB）

```
ComfyUI/
└── models/
    │
    ├── diffusion_models/
    │   └── MiniMaxH3/
    │       └── minimax_h3_fl2va_pruned_int8_convrot.safetensors
    │
    ├── text_encoders/
    │   └── qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors
    │
    └── vae/
        └── MiniMaxH3/
            ├── minimax_h3_video_vae_fp16.safetensors
            └── minimax_h3_audio_vae_fp32.safetensors
```

### 核心模型说明

- 主模型：`minimax_h3_fl2va_pruned_int8_convrot.safetensors`（剪枝 INT8 版 H3，社区快速工作流采用）
- 文本编码器：`qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors`（NVFP4/AWQ 量化版）
- 双 VAE：视频 VAE（fp16）+ 音频 VAE（fp32）

> 43GB 是完整快速栈的文件体积（模型 + 编码器 + 两个 VAE），**并非全部都要进显存**，配合 INT8 量化与 SageAttention 可跑在 24GB 卡上。

### 为什么选这个版本？

RTX A5000 是 **24GB 不是 48GB**，所以第一阶段优先考虑：

- ✅ INT8 + Pruned
- ❌ 完整模型 + 大编码器 + REF2VA + 高分辨率 + 大量参考图

一上来把东西全堆进去，容易把显存和内存搞爆。

---

## 六、插件

第一阶段只需要两个：

1. **KJNodes** —— SageAttention、图片尺寸处理、H3 相关节点
2. **rgthree-comfy** —— LoRA 管理

H3 Fast 工作流明确要求这两个节点包。

---

## 七、SageAttention

- 版本：**2.x**
- 依据：A5000 属于 **Ampere** 架构
- 原因：H3 的注意力计算非常吃资源，SageAttention 是社区 H3 工作流明确的内存 + 速度优化项。

---

## 八、工作流演进路线（按阶段推进）

| 阶段 | 工作流 | 用途 | 备注 |
|---|---|---|---|
| 第一阶段 | **T2V**（文字 → H3 → 视频+音频） | 测试机器能否稳定跑 H3 | 探路用 |
| 第二阶段 | **I2V**（首帧 + Prompt → H3 → 视频+音频） | 做连续剧的主力 | 核心 |
| 第三阶段 | **FL2VA**（首帧 + 尾帧 + Prompt → H3 → 视频+音频） | 首尾帧控制 | 做连续剧最终主力 |
| 第四阶段 | **Ref2VA**（人物/场景参考图 + Prompt → H3） | 角色和场景控制更强 | 显存/模型要求更高，先别急 |

> **Ref2VA 说明**：是额外的模型负担，FL2VA 更轻。H3 Multishot 资料明确说明 REF2VA 更重，所以放最后。

---

## 九、H3 Multishot（连续镜头）

> **仓库地址**：<https://github.com/jlucasmcrell/ComfyUI-H3-Multishot>（MIT 协议，作者 jlucasmcrell）

专用于解决：多个 H3 镜头连续生成，并把音频一起串起来，渲染为一条**连续无剪辑**的"单镜头"视频（无可见剪辑、无镜头间色偏、音频不断裂）。

```
Shot 1 → Shot 2 → Shot 3 → Shot 4 → 最终连续视频
```

后面镜头可接前一个镜头的**最后一帧**，是做连续短剧的关键。

### 版本要求

- 使用前提：ComfyUI **v0.30.0+**（原生支持 MiniMax-H3），官方推荐 **v0.34+**
- 当前版本：v2.7.x（README 标注 v2.6.5，更新日志已含 2.7.0/2.7.1/2.7.2）

### 两套镜头拼接机制

| 机制 | 说明 | 依赖 |
|---|---|---|
| `first_frame` | 帧中继，走 FL2VA 的图生视频条件路径 | 零第三方依赖（CORE 版工作流） |
| `context_pin` | 原始 latent 钉扎 | 需额外装 `ComfyUI-H3-Motion-Context`（默认"无缝拼接"方案） |

附带工作流：`H3_Seamless_Chain_v2`（完整版）、`H3_Seamless_Chain_CORE`（无第三方依赖）、`H3_Extend_Take`（延长拍摄）。

### 24GB 显存支持

- 通过 **H3StudioSwitches（VRAM/SPEED 开关面板）** 的 **activation-reserve 控制**（自动/手动）来预留激活内存，已在 24GB 卡上实测；**没有叫 `activation_reserve_gb` 的精确参数**
- 16–24GB 卡官方推荐默认配置：**736x1280 分辨率、192 帧、14 steps、`curve-Q5_1` 模型**
- 长链内存优化：`low_ram_master`（逐段写磁盘暂存，降低系统 RAM 峰值）+ driver-headroom 规则（VRAM 接近满载时自动流式传输部分权重，避免卡顿/崩溃）

### GGUF 路线（可选，第三阶段）

- 模型加载器是**双格式**（safetensors + GGUF），但需额外安装 `ComfyUI-GGUF` 并运行一次架构补丁：

```bash
cd ComfyUI/custom_nodes/ComfyUI-H3-Multishot
python apply_gguf_arch_patch.py
```

- 文本编码器 GGUF 需用 `-mmproj-*.gguf` 视觉侧车，且必须用仓库自带的 **H3 CLIP Loader** 加载（不能用 ComfyUI 自带的 `CLIPLoaderGGUF`）
- 24GB 卡量化选型：

| GGUF 量化 | 适配显存 |
|---|---|
| Q8_0 | 32GB |
| **Q5_1**（优先） | 24–32GB |
| Q4_0 | 16GB |

> 24GB 卡优先选 **Q5_1**，而不是 Q8。

---

## 十、连续剧分镜生产法

不要直接"生成 30 秒剧情"，而是拆镜头：

```
SC01
│
├─ SHOT01  客栈外景          5秒
├─ SHOT02  林萧推门          5秒
├─ SHOT03  苏婉抬头          5秒
├─ SHOT04  林萧近景"你怎么在这里？"  5秒
├─ SHOT05  苏婉近景"我一直在等你。"  5秒
└─ SHOT06  两人对视          5秒
```

然后逐镜交给 H3，由 H3 Multishot 串联成连续视频 + 音频。

### 镜头状态机（防止道具 / 空间 / 动作接不上）

每个镜头按「状态机」写法约束，避免上一镜和下一镜对不上：

- 格式：**上一镜结束状态 → 本镜开始状态 → 本镜唯一动作 → 本镜结束状态 → 下一镜衔接**
- 每镜只做**一件事**，动作过多会导致视频模型不知道先做什么
- 标注**禁止变化**项：如「刀已落地，本镜角色手里不能有刀」「门在画面右侧，不能变成左侧」「手机屏幕文字单独出 UI 图，不在视频里生成」

> 参考 short-drama-agent 的连续性状态机方法，不装该插件，方法照用。

### 随机种子管理（seed）

- 每个镜头生成时**记录 seed**（写进分镜表备注或文件名后缀，如 `第1集_镜03_成片_s12345.mp4`）
- **失败重跑必须保持相同 seed + 相同输入**（同一 master / 首帧 / Prompt），只调整参数或 Prompt，才能定位问题；换 seed = 换一张完全不同的结果，无法对照
- 同一镜多次尝试时，优先**固定 seed 调 Prompt**，而不是每次随机撞

### 单镜生成 SOP（拿到分镜后怎么落地一镜）

以 `第1集_镜03` 用 I2V / FL2VA 生成为例：

1. **确认资产**：本镜用到的角色 / 场景 master 已锁定存在（`00_角色素材/林萧/linxiao_master.png`、`01_场景素材/客栈/客栈_master.png`）
2. **生成首帧**（I2V、FL2VA 都必填）：用 ComfyUI 文生图（SDXL / Flux / Qwen-Image 等任一支 checkpoint）按首帧 Prompt 生成，或从 H3 T2V 产物中抽一帧；**分辨率直接用 736×1280（H3 生成规格），与 H3 输入同比例，避免缩放变形**；保存为 `02_分镜/第1集/第1集_镜03_首帧.png`
3. **生成尾帧**（仅 FL2VA）：同上按尾帧 Prompt 生成、同样规格，保存为 `第1集_镜03_尾帧.png`
4. **装填工作流**：加载 H3 I2V / FL2VA 模板 → 挂上角色 / 场景参考图 → 输入首尾帧 → 粘贴 H3 Video Prompt + 音频 Prompt → 设置分辨率 / 帧数 / 步骤数
5. **记录 seed**：生成前记下 seed（第 2 步也记文生图 seed），失败重跑才有对照
6. **审片**：按第十六章逐镜审片，不过则改 Prompt 或参数重跑，过则进入下一镜

> 首帧 / 尾帧图是 H3 的输入素材，属于 `02_分镜/第X集/` 产物，和 master 基准图分开存放（见第十二章）。

---

## 十一、H3 对白写作规范

不要只写"两个人聊天"，必须明确：

- **Speaker**（谁说的）
- **Voice**（声音特征）
- **Emotion**（情绪）
- **Dialogue**（台词）

示例：

```
林萧："你怎么会在这里？"
苏婉："我一直在等你。"
林萧："等我？"
苏婉："等你回来。"
```

> **关键**：声音连续性需要在每个镜头里持续提供具体、稳定的声音描述，不要只写 "same voice"。

---

## 十二、素材与产物保存规范（output 目录与命名）

章节一多，图片/视频几十上百个，**必须靠目录分层 + 统一命名区分，不能靠生成时间戳认图**。

### 1. ComfyUI 默认保存位置

- 所有图片、视频默认输出到 `ComfyUI/output/`（节点保存、前端右键 Save、`Ctrl+S` 都到这里）
- 默认文件名带 `_00001` 序号 + 时间戳，不设置前缀会完全分不清是谁、哪一集、哪一镜

### 2. 三种保存姿势（按推荐顺序）

| 方式 | 操作 | 结果 |
|---|---|---|
| ① 节点 `SaveImage` / `SaveVideo` 的 `filename_prefix`（**推荐**） | 前缀里带 `/` 路径，ComfyUI 自动建子目录 | 自动归档到 `output/分类/对象/文件` |
| ② 前端右键图片/视频 → Save | 保存到 `output/` | 带序号，需手动归类 |
| ③ 前端 `Ctrl+S` | 保存当前预览 | 同上 |

> `filename_prefix` 开头加 `!` 可**跳过自动序号**，适合固定名文件，如 `!00_角色素材/林萧/linxiao_master.png`。

### 3. output 目录分层规范（三级：环节 → 对象 → 文件）

```
ComfyUI/output/
├── 00_角色素材/                  ← 第一级：素材环节
│   ├── 林萧/                     ← 第二级：对象（角色名）
│   │   ├── linxiao_正面.png
│   │   ├── linxiao_侧面.png
│   │   ├── linxiao_背面.png
│   │   ├── linxiao_全景.png
│   │   ├── linxiao_玉坠_正面.png       ← 配饰三视图
│   │   ├── linxiao_佩剑.png            ← 随身道具参考图
│   │   └── linxiao_master.png
│   └── 苏婉/
├── 01_场景素材/
│   ├── 客栈/
│   │   ├── 客栈_正面.png
│   │   ├── 客栈_侧面.png
│   │   ├── 客栈_背面.png
│   │   ├── 客栈_全景.png
│   │   └── 客栈_master.png
│   └── 庭院/
├── 02_分镜/                       ← 按剧集/章节分
│   └── 第1集/
│       ├── 第1集_镜01_首帧.png
│       ├── 第1集_镜01_尾帧.png
│       └── 第1集_镜01_成片.mp4
└── 03_成片/
    └── 第1集_最终成片.mp4
```

> 若同时制作**多部剧**，第一级再加一层 `剧名/`（即 `output/剧名/00_角色素材/...`），各剧资产互不干扰；单剧可省略。

### 4. 统一命名规范

| 产物 | 命名规则 | 示例 |
|---|---|---|
| 角色三视图 | `角色名_正面/侧面/背面.png` | `linxiao_正面.png` |
| 角色全景图 | `角色名_全景.png` | `linxiao_全景.png` |
| 配饰三视图 | `角色名_配饰名_正面/侧面/背面.png` | `linxiao_玉坠_正面.png` |
| 随身道具参考图 | `角色名_道具名.png` | `linxiao_佩剑.png` |
| 场景关键道具 | `场景名_道具名.png` | `客栈_剑架.png` |
| 角色基准图 | `角色名_master.png` | `linxiao_master.png` |
| 场景素材 | `场景名_正面/侧面/背面/全景.png` | `客栈_全景.png` |
| 场景基准图 | `场景名_master.png` | `客栈_master.png` |
| 镜头首/尾帧 | `第X集_镜YY_首帧/尾帧.png` | `第1集_镜03_首帧.png` |
| H3 逐镜成片 | `第X集_镜YY_成片.mp4` | `第1集_镜03_成片.mp4` |
| 剪辑总成片 | `第X集_最终成片.mp4` | `第1集_最终成片.mp4` |

> 集/章节用阿拉伯数字（`第1集`），镜头用两位补零（`镜03`），保证排序不乱。

### 5. 各环节 `filename_prefix` 填写示例

| 环节 | 前缀示例 |
|---|---|
| 角色三视图/全景图 | `00_角色素材/林萧/linxiao_正面` |
| 配饰/道具素材 | `00_角色素材/林萧/linxiao_玉坠_正面` / `00_角色素材/林萧/linxiao_佩剑` |
| 角色 Master | `!00_角色素材/林萧/linxiao_master.png` |
| 场景素材 | `01_场景素材/客栈/客栈_全景` |
| 分镜首/尾帧 | `02_分镜/第1集/第1集_镜03_首帧` |
| H3 成片 | `02_分镜/第1集/第1集_镜03_成片` |
| 剪映最终成片 | `03_成片/第1集_最终成片` |

### 6. 避坑

- **不要**把 master 图混在分镜图里：基准图一改，所有镜头跟着变
- **不要**用 `linxiao_final.png` / `最终版2.png` 这类名字，一律固定 `_master`
- 视频产物别落进 `temp/`（临时目录重启会被清），确认输出到 `output/`
- 镜头对不上时，靠 `第X集_镜YY` 前缀立刻定位对应的首尾帧和成片
- 改好的 H3 工作流 JSON（`导出 API 格式`）导出备份到项目目录（如 `04_工作流/`），避免换机 / 重装 / 误改后丢失调好的参数

---

## 十三、角色一致性

不幻想 H3 自己记住角色，正确方式：

每个角色都要生成**三视图 + 全景图**素材包（同一角色、同一服装、同一造型），**配饰和随身道具单独出图**：

1. 三视图：正面 / 侧面 / 背面（如 `linxiao_正面.png` / `linxiao_侧面.png` / `linxiao_背面.png`）
2. 全景图：全身完整立绘，含服饰细节（如 `linxiao_全景.png`）
3. **配饰三视图**：固定配饰（首饰 / 头饰 / 眼镜 / 挂坠等）单独出三视图，同一配饰同一佩戴位置（如 `linxiao_玉坠_正面.png`）
4. **随身道具参考图**：角色固定持有的道具（剑 / 扇子 / 手机 / 烟杆等）单独出图（如 `linxiao_佩剑.png`）；道具状态（出鞘 / 电量 / 开合）要与第十章状态机的「禁止变化」一致
5. 可选补充：45° / 半身 / 表情
6. 确定一张 **Master Character Image**（基准图，如 `linxiao_master.png`）
7. 以后所有镜头都以它为基准
8. **验收锁定**：核对三视图/全景图中角色脸、发型、服装、配饰、道具是否一致，细节是否完整；不合格重生成，确认后锁定 master 不再改动

---

## 十四、场景一致性

每个场景同样生成**三视图 + 全景图**素材包（同一场景、同一机位体系）：

1. 三视图：正面 / 侧面 / 背面（同一场景的不同机位视角，如 `客栈_正面.png` / `客栈_侧面.png` / `客栈_背面.png`）
2. 全景图：场景全貌参考（如 `客栈_全景.png`）
3. 关键陈设 / 场景道具单独出图（如 `客栈_剑架.png` / `客栈_酒坛.png`），固定位置与朝向，配合第十章状态机防止道具在镜头间挪位
4. 然后确定场景 Master：

```
客栈_master.png
庭院_master.png
街道_master.png
房间_master.png
```

生成时：**人物 Master + 场景 Master + 分镜 Prompt** → 交给 H3。

> 场景素材同样要验收：三个视角 + 全景中场景布局、机位体系、灯光是否一致，确认后再锁定为场景 master。

---

## 十五、Codex / CodeBuddy 编剧与分镜（无需开发）

编剧和分镜直接用 **Codex / CodeBuddy**（本工具）完成，例如输入：

> "这是第 1 章，帮我改成 3 分钟短剧。"

要求输出（**固定 10 项**，作为与 ComfyUI/H3 对接的标准格式）：

1. 剧集结构
2. 人物
3. 场景
4. 道具
5. 台词
6. 镜头
7. 首帧 Prompt
8. 尾帧 Prompt
9. H3 Video Prompt
10. 音频 Prompt

### 推荐：可直接套用的编剧指令模板

把下面这段复制给 Codex/CodeBuddy：

> 角色：你是短剧编剧 + 分镜师。
> 任务：把下面的小说段落改写成一部 X 分钟短剧，并输出标准分镜表。
> 规则：
> 1. 每镜 3~10 秒，在分镜表中标注镜头时长；
> 2. 每个镜头必须包含：镜头号、景别、画面描述、人物状态、对白（Speaker / Voice / Emotion / Dialogue）、首帧 Prompt、尾帧 Prompt、H3 Video Prompt、音频 Prompt；
> 3. 人物与场景命名必须和既有 Master 图一致（如 `linxiao_master.png` / `客栈_master.png`）；
> 4. 每个角色和场景都要配套三视图（正面/侧面/背面）+ 全景图素材，角色配饰（首饰/头饰/眼镜等）和随身道具（剑/扇子/手机等）单独出三视图/参考图；
> 5. 输出用表格或 JSON，便于直接复制进 ComfyUI；
> 6. 每个镜头标注「状态机」：上一镜结束状态 → 本镜唯一动作 → 本镜结束状态 → 下一镜衔接 → 禁止变化（见第十章）。
> 小说原文：
> [粘贴小说段落]

### 输出如何对接下游

- 分镜表 → 复制到 ComfyUI（角色/场景 Master + 首尾帧 Prompt）
- H3 Video Prompt + 音频 Prompt → 逐镜交给 H3 生成
- 全部镜头 → H3 Multishot 串联成连续视频

**不需要写程序**，把结果复制到 ComfyUI 即可。

### 可选：MiniMax-H3-Codex-Drama（Codex 自动化插件）

> **仓库地址**：<https://github.com/chiphoton/MiniMax-H3-Codex-Drama>（MIT 协议，作者 chiphoton，25 commits）

如果想连"分镜 + 转 H3 Prompt"都自动化，可考虑这个 **Codex-first 视频生产插件**：

- **输入**：自然语言故事/简报（小说式情节）
- **内部流程**：解析设定（角色/场景）→ 生成故事板/关键帧 → 逐镜路由到 MiniMax H3 → 本地 ComfyUI 生成 → 后期（剪辑/配音/字幕/音效/QC）→ 输出可复现成片
- **内置 10 个 skills**：`drama-producer`（完整生产/续跑）、`profile-distiller`（从参考视频提取风格）、`adviser`（选工作流/优化提示词/诊断失败）、T2V/I2V/R2V/audio、`video-editor`、`comfyui`、`qwen-image-edit`
- **衔接方式**：内置 ComfyUI MCP 连接（`comfyui-mcp`），让 Codex 直接提交/监控/拉取本地 ComfyUI 任务

**安装**（Codex 插件市场）：

```bash
codex plugin marketplace add chiphoton/MiniMax-H3-Codex-Drama
codex plugin add minimax-h3-drama@chiphoton
codex plugin add h3style@chiphoton   # 可选：官方 H3 风格技能
```

或仅装 skills（不带 MCP 连接）：

```bash
npx skills add chiphoton/MiniMax-H3-Codex-Drama --all -g -a codex -y
```

> 实际安装后共 **20 个 skills**（除上述 10 个外，还有 `short-drama-screenwriter` / `film-shot` / `multi-shot` / `voiceover-direction` / `subtitle-correction` 等），本机已装于 `skills/minimax/installed/` 下。

**依赖与注意**：

- 本地环境需：Node.js + npm、ComfyUI 运行在 `localhost:8188`、**FFmpeg/FFprobe**（剪辑/合成/QC）
- **Turbo 模式默认开启**：需装 `ComfyUI-MiniMax-H3-Turbo` 节点 + LoRA `minimax_h3_turbo_v4_step600_ema.safetensors`；用 `[turbo=false]` 可退回原始 20 步工作流
- 每次生成记录 prompt/hash/workflow 到项目台账，支持断点续跑
- **定位提醒**：这是"第四阶段后的进阶自动化"，第一阶段先把 T2V → I2V → FL2VA 手动跑通再考虑

### 可选：本机已装的 Codex Skills（更细的编剧规范）

`skills/` 目录下已有 5 个可复用的 Codex/CodeBuddy Skill（本地文件，无需安装），把对应 `SKILL.md` 喂给 Codex 即可启用，作为上述编剧指令模板的**细化规范**：

| Skill | 作用 | 对接章节 |
|---|---|---|
| `script_rewriter` | 小说 → 格式化剧本（`## S01` 场景头 + 逐角色分行对白 + 旁白单独成行） | 本章编剧 |
| `extractor` | 提取角色 / 场景 / **道具**（含道具图片提示词），同名去重复用 | 第十三 / 十四章（含配饰与随身道具） |
| `storyboard_breaker` | 分镜拆解：一镜一人说、对话 5-8 秒、`scene_type` 路由、`speaker_id`、3 秒分段 `video_prompt` | 第十章（分镜生产法 + 状态机） |
| `voice_assigner` | 音色分配：一角色一音色、`speaker_id` 全局唯一、跨集锁定 | H3 对白 / 配音 |
| `grid_prompt_generator` | 角色 / 场景 / **宫格图**三类图片提示词规范（首帧 / 首尾帧 / 多参考，每格 960×540） | 第十三章（三视图一致性检查） |

> 定位：主流程仍按 H3 固定工作流手工跑，这些 Skill 只用于让 Codex 产出更规范的分镜/提示词；`grid_prompt_generator` 的宫格图模式可一次生成多视角网格，用于核对角色/配饰/道具是否一致。

### 分镜表示例（Codex 输出长这样）

以 **3 镜**示例说明标准产出（角色 `林萧` / `苏婉`，场景 `客栈`），Codex 应按此骨架产出整集 JSON：

```json
{
  "剧集结构": { "剧集": 1, "场景数": 1, "镜头数": 3, "总时长": "约15秒" },
  "人物": [
    { "id": "linxiao", "名": "林萧", "master": "00_角色素材/林萧/linxiao_master.png", "配饰": ["玉坠"], "随身道具": ["佩剑"] },
    { "id": "suwan", "名": "苏婉", "master": "00_角色素材/苏婉/suwan_master.png" }
  ],
  "场景": [ { "id": "kezhan", "名": "客栈", "master": "01_场景素材/客栈/客栈_master.png" } ],
  "道具": [ { "名": "佩剑", "参考图": "00_角色素材/林萧/linxiao_佩剑.png", "状态": "未出鞘" } ],
  "台词": [
    { "镜": 2, "speaker": "linxiao", "voice": "青年男声，低沉平稳", "emotion": "意外", "dialogue": "你怎么会在这里？" },
    { "镜": 3, "speaker": "suwan", "voice": "青年女声，清柔", "emotion": "坚定", "dialogue": "我一直在等你。" }
  ],
  "镜头": [
    { "镜": 1, "景别": "全景", "时长": 5, "描述": "客栈外景，林萧推门入", "状态机": "门外 → 推门 → 入门 → 下一镜面对苏婉" },
    { "镜": 2, "景别": "近景", "时长": 5, "描述": "林萧看到苏婉，惊讶", "状态机": "入门 → 驻步 → 抬手指 → 苏婉注视 → 禁止变化：佩剑出鞘" },
    { "镜": 3, "景别": "近景", "时长": 5, "描述": "苏婉起身回应", "状态机": "坐 → 起身 → 注视 → 对视 → 禁止变化：位置移动" }
  ],
  "首帧Prompt": "镜1：客栈外景清晨，林萧推门；镜2：林萧近景惊讶；镜3：苏婉近景起身",
  "尾帧Prompt": "镜1：林萧已入门；镜2：林萧手指前方；镜3：苏婉与林萧对视",
  "H3VideoPrompt": "镜1：推门动作，门随动作打开；镜2：林萧惊讶抬头，嘴部微动；镜3：苏婉起身，说话时口型同步",
  "音频Prompt": "镜1：开门声+环境声；镜2：对白（speaker/voice/emotion 见台词表）；镜3：对白（同上）+轻微环境声；BGM：古风弦乐，慢"
}
```

> 实际使用时逐镜拆成单镜数据喂给 ComfyUI/H3；`id`（即 `speaker_id`）全局唯一且跨集不变。

---

## 十六、剪辑（剪映）

```
H3 Shot01
H3 Shot02
H3 Shot03
H3 Shot04
H3 Shot05
       ↓
      剪映
       ↓
字幕 / 转场 / BGM / 音效 / 片头 / 片尾
       ↓
最终 MP4
```

H3 已有原生音频，**TTS 不作为主流程**。

### 何时才需要额外 TTS？

只有出现以下情况才加 CosyVoice / Fish Speech / GPT-SoVITS：

- H3 声音不稳定
- 角色声音变化
- 对白太长
- 音画同步不好
- 需要重新配音

否则**不要加**。

### 音频 / BGM / 字幕分工

| 声音层 | 来源 |
|---|---|
| 对白 + 环境声 | H3 原生音频（靠分镜的音频 Prompt / Speaker / Voice / Emotion 控制） |
| BGM | **剪映音乐库**搜索关键词（"古风 / 悬疑 / 温馨"）直接配，最快；分镜里的 BGM 意图（如"古风弦乐、慢、60-70 BPM"）当参考，不指望 H3 出 BGM；需原创再考虑 Suno / 剪映"AI 配乐" |
| 音效 | 敲门 / 开门 / 刀剑出鞘等 H3 不一定出，缺口用**剪映音效库**补 |
| 字幕 | **剪映"智能字幕"**自动识别对白生成，再手动校对错字（人名 / 古风词易错）；分镜台词本来就是逐句的，可直接对照粘贴校准 |

### 逐镜审片（QC 清单，每镜逐项勾）

H3 成片后按 `第X集_镜YY` 逐镜检查，**所有项通过才算过**：

- [ ] 画面清晰，无闪烁 / 无大面积伪影 / 无运动拖影
- [ ] 角色形象与 master 一致（脸 / 发型 / 服装 / 配饰 / 道具）
- [ ] 道具状态符合状态机「禁止变化」（佩剑未出鞘 / 手机电量 / 门朝向）
- [ ] 空间关系正确（门在画面左侧没变 / 桌椅朝向没换）
- [ ] 口型与台词基本对得上（明显错位 → 重跑）
- [ ] 声音正常（无爆音 / 无杂音 / 音量与前后镜一致）
- [ ] 时长在分镜计划 ±20% 内

**只重写失败的镜头**，保留已通过的镜头，避免整段重来。

### 成片规格（短剧标准竖屏）

| 项 | 规格 | 说明 |
|---|---|---|
| 比例 | **9:16 竖屏** | 短剧主流分发比例（抖音 / 快手 / 视频号） |
| 分辨率 | 生成 **736×1280**，导出 **1080×1920** | H3 按 736×1280 生成（24GB 卡推荐值），剪映最终导出 1080×1920 |
| 帧率 | 25 / 30 fps 全片统一 | 混帧率会导致剪辑与字幕卡顿 |
| 时长 | 单集 1~3 分钟 | 免费集建议 60~90 秒，付费解锁集可更长 |

> 首帧 / 尾帧文生图、以及剪辑调色全程都用同一比例，不要混入横屏素材（横屏只能做上下黑边裁切，损失画质）。

### 短剧通用包装（成片形态）

短剧除了剧情镜头，还要有**统一包装元素**，在剪映里做，一套样式全集复用、不要每集重新设计：

| 元素 | 做法 | 复用方式 |
|---|---|---|
| 片头（3~6 秒） | 剧名 + Logo + 开场 hook（放本集最爆的一句台词画面） | 存剪映草稿模板，每集只换画面 |
| 集数角标 | 右上 / 左上角"第 X 集"小字，统一样式 | 存全局素材 |
| 章节标题卡 | 转场标题页（"三年后""客栈内"） | 一剧一套样式 |
| 片尾（3~5 秒） | 下集预告（用下一集首镜）+ 关注 / 完播引导 | 每集拼装 |
| 字幕样式 | 字体 / 描边 / 位置统一，正文色 + 强调色 | 存字幕模板 |

> 包装元素和角色 / 场景 master 一样属于"一次性资产"：第一集把样式定好，后面全集直接复用，审片时只检查剧情镜头，不重复查包装。

---

## 十七、Turbo LoRA（最后阶段）

> **仓库地址**：<https://github.com/Larryvrh/ComfyUI-MiniMax-H3-Turbo>（Apache-2.0，作者 Larryvrh，538 stars）

- 作用：标准 H3 25 steps → **Turbo 4~8 steps**，大幅提高生成速度
- **前提**：标准 H3 跑通后（质量、显存、音频、角色、连续性全部正常）再装

### 推荐参数（官方 README）

| 项目 | 建议 |
|---|---|
| 推荐权重 | `minimax_h3_turbo_v4_step600_ema.safetensors`（v4/600 步） |
| 步骤数 | **4~8 步**，其中 6~8 步画质明显更好；**超过 8 步不再提升，反而出现锐化伪影** |
| 强度 | 默认 **1.0**；模糊重影 → 0.8~0.95；过锐颗粒 → 1.05~1.2 |
| 调度器 | 必须使用 **simple** |
| 注意事项 | v4 在 4 步 + 大幅快速运动时可能出现运动拖影/鬼影，此时改用 6~8 步或退回旧版 v1~850 |

### 24GB 显存适配

- 节点**流式加载基础模型**，峰值显存远低于 33B 模型原始体量
- `low_vram = off`（默认）：运行时注入 LoRA，画质最锐，但略增峰值显存
- `low_vram = on`：LoRA 合并进权重，**显著降低峰值显存**；但 INT8/FP8/Pruned 基座上画质会略软（24GB 卡建议开启并控制分辨率/帧数）

### 安装

```bash
cd ComfyUI/custom_nodes
git clone https://github.com/Larryvrh/ComfyUI-MiniMax-H3-Turbo
```

- 也可用 **ComfyUI-Manager** 搜索 `MiniMax-H3 Turbo` 一键安装
- LoRA `.safetensors` 放入 `ComfyUI/models/loras/`
- 无严格 ComfyUI 版本限制，新旧版本自动适配（新版走 `ModelSamplingAV`，旧版 Turbo Sampler 自动分视频/音频调度）
- 仓库内附 `example_workflows/` 可拖入查看连接方式

---

## 十八、关于 20 分钟长视频（概念纠正）

不要让 H3 一次生成 20 分钟，正确方式：

```
20分钟短剧
    ↓
100～200个镜头
    ↓
每镜 3～10秒
    ↓
H3 逐镜生成
    ↓
剪辑
    ↓
20分钟成片
```

参考计算：20 分钟 ≈ 1200 秒，平均 6 秒/镜 ≈ **200 个镜头**。这才是正常的 AI 短剧生产方式。

---

## 十九、硬件路线（A5000 24GB 分阶段）

| 阶段 | 方案 |
|---|---|
| 第一阶段 | H3 Pruned INT8 + Qwen3-VL NVFP4/AWQ + 双 VAE + SageAttention |
| 第二阶段 | H3 I2V + FL2VA |
| 第三阶段 | H3 Multishot（24GB 卡建议搭配 Q5_1 GGUF，见第九章） |
| 第四阶段 | Turbo LoRA |



## 二十、执行顺序（10 步）

1. **用 Codex/CodeBuddy 做编剧和分镜**：输入小说 → 输出 10 项标准分镜表（见第十五章）。**这一步就先定下角色 / 场景命名清单**（如 `linxiao` / `suwan` / `kezhan`），第 6 步按清单生成素材，避免名字对不上
2. **安装/更新 ComfyUI ≥ 0.30.0**
3. **下载模型**：H3 Pruned INT8 + Qwen3-VL NVFP4/AWQ + Video VAE + Audio VAE（约 43GB），按第五章目录结构放置
4. **安装插件**：KJNodes + rgthree-comfy + SageAttention
5. **先跑 H3 T2V**（验证机器能否稳定跑）
6. **生成角色 / 场景素材并验收锁定**：为每个角色和场景生成三视图（正面/侧面/背面）+ 全景图 + Master 基准图，**配饰和随身道具单独出图**（见第十三/十四章），按第十二章目录规范保存；逐一验收（视角是否齐全、同角色同服装/配饰/道具是否一致、细节是否完整），不合格重生成，全部通过后锁定 master 不再改动
7. **跑 H3 I2V**（首帧）
8. **跑 H3 FL2VA**（首尾帧控制）
9. **安装 H3 Multishot**，开始做双人对话 / 多人会议 / 激烈争吵 / 连续剧
10. **最后再上 Turbo LoRA**

---

## 二十一、关键要点速记

- 编剧和分镜 = Codex / CodeBuddy，固定输出 10 项标准格式
- ComfyUI ≥ 0.30.0 才有原生 H3 支持
- 24GB 卡 = INT8 + Pruned + SageAttention，先轻后重
- 连续剧主力 = 首帧/首尾帧控制，不是纯 T2V
- 对白必须写 Speaker / Voice / Emotion / Dialogue
- 角色、场景各自固定 Master 图，不指望模型自动记住
- 每个角色、每个场景都要生成三视图（正面/侧面/背面）+ 全景图素材；配饰与随身道具单独出图
- 素材/产物一律按 output 目录分层 + 统一命名保存，不靠时间戳认图（见第十二章）
- 长视频靠逐镜生成（每镜 3~10 秒）+ 剪映拼接，不靠一次性生成
- TTS / Turbo / Ref2VA / GGUF 全部放后面阶段
- 多集生产：master 图 / `speaker_id` / 音色全剧复用并锁定，见附录 A
- 声音分工：H3 出对白+环境声，BGM 剪映配，音效缺口剪映补，字幕智能识别+校对（第十六章）
- 每镜记录 seed，失败重跑保持相同 seed + 相同输入（第十章）
- 首帧 / 尾帧图由 ComfyUI 文生图生成，与 master 分开存放（第十章单镜 SOP）
- 包装元素（片头 / 角标 / 片尾 / 字幕样式）一套全集复用（第十六章）
- 一剧一张剧集设定卡，编剧前先读、写完回写（附录 A）
- 成片统一 9:16 竖屏：H3 生成 736×1280，剪映导出 1080×1920（第十六章）
- 每镜按 QC 清单逐项勾过再进下一镜（第十六章）
- 首集先用 3~5 镜走通全链路再放量，防止整集返工（附录 A）
- 出问题先查附录 B（常见问题排查）

---

## 附录 A：多集 / 整剧生产要点

前面章节围绕"单集"展开，短剧实际是 20~50 集，跨集必须做到：

1. **资产全剧复用**：角色 / 场景 / 配饰 / 道具的 master 图在**整部剧所有集**共用，一旦锁定**禁止中途改动**（改一个等于所有集重跑）
2. **角色库与出场表**：`extractor` 提取出的角色 / 场景 / 道具就是全剧资产库；先建**出场表**（哪个角色在哪几集出场），避免镜头里出现没建过资产的角色
3. **`speaker_id` 跨集锁定**：`voice_assigner` 分配的音色全剧不变，同一角色在 1 集和第 40 集必须同一个 `speaker_id`、同一段 voice 描述
4. **场景库复用**：同一场景多集复用同一套三视图 + master，不用每集重出
5. **道具跨集状态**：剑是否出鞘、手机电量、门窗朝向等跨集也要符合第十章状态机，不能上一集坏了下一集好了
6. **归档隔离**：按第十二章 `第X集` 目录归档，多集互不混；集与集之间的镜头串接（上集结尾 → 下集开头）要在分镜里显式衔接
7. **允许的变化**：剧情需要换装 / 场景改造时，必须**重新生成三视图并锁定为新 master**，旧 master 保留给旧集使用，不要悄悄改共用资产
8. **剧集设定卡（剧情连续性）**：一剧维护一张设定卡（交给 Codex 维护），含世界观规则、角色关系、时间线、关键道具去向、已埋伏笔、已交代信息；**每次编剧分镜前先让 Codex 读设定卡**，写完再回写更新，避免跨集剧情 / 关系 / 时间线冲突
9. **首集全链路试水（防返工）**：正式放量前，先用第 1 集的 **3~5 个镜头**走完"分镜 → 素材 → H3 → 审片 → 剪映 → 导出"全链路，确认流程与成片风格没问题，再回头补齐整集；**不要**一上来整集大批量生成，否则流程一旦有问题整集返工

---

## 附录 B：常见问题排查（FAQ）

| # | 问题 | 排查路径 |
|---|---|---|
| 1 | **显存 OOM（CUDA out of memory）** | 先降分辨率 / 帧数 → 开 `H3StudioSwitches` 的 activation-reserve → 确认用的是 INT8 + Pruned 模型 → 确认 SageAttention 已生效 → 还不行上 Turbo 的 `low_vram=on` 或 Q5_1 GGUF |
| 2 | **H3 没声音 / 声音不对** | 确认音频 VAE 已加载且工作流接了 AudioVAE 解码节点；检查每个镜头是否都写了音频 Prompt / Speaker / Voice；换 seed 重跑对比 |
| 3 | **角色不像** | 确认用的是角色 master + 首帧参考而非纯文字；调高参考图权重；配饰 / 道具要单独出图并在镜头里指明 |
| 4 | **Multishot 报错** | ComfyUI 需 ≥ 0.30.0（官方推荐 0.34+）；先用零依赖的 CORE 版；`context_pin` 需装 `ComfyUI-H3-Motion-Context`；undefined 类报错多为版本过旧 |
| 5 | **SageAttention 编译 / 不生效** | 确认 Python / CUDA 版本与预编译包匹配；装不上就先退回普通 attention 把流程跑通 |
| 6 | **GGUF 报错** | 确认已执行 `apply_gguf_arch_patch.py`；文本编码器必须用 H3 CLIP Loader 加载（不能用 ComfyUI 自带 `CLIPLoaderGGUF`）；`-mmproj` 视觉侧车不能少 |
| 7 | **生成很慢 / 卡顿** | 确认 SageAttention 生效；曲线模型优先 Q5_1；长链开 `low_ram_master` 防内存爆；Turbo 放最后再上 |
| 8 | **视频噪点 / 色偏** | 低显存 / INT8 下属正常现象，优先控制分辨率帧数而不是盲目堆参数；逐镜审片后只重跑失败镜头 |
