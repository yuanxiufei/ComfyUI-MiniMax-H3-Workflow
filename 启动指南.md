# 启动指南

把 `小说 → 剧本 → 分镜 → 图片素材 → 视频成片` 一条链在本地跑起来。

> 本文所有命令都在**项目根目录**执行，目录即 `ComfyUI-MiniMax-H3-Workflow/`。

---

## 1. 这是什么

一条 `pipeline.py` 编排的流水线，共 9 步：

```
script_rewriter → extractor → storyboard_breaker → voice_assigner
→ grid_prompt_generator → qc → build → submit → video
```

- 前 6 步是**确定性数据层**：只读/写 `剧本/` 下的 JSON 与文本，不消耗算力。
- `qc` 是**分镜质量门禁**（`qc_storyboard.py`，P0/P1/P2），默认只报告不阻断；
  传 `--qc-block` 时存在 P0 致命项（台词塞不下预算/单镜超长）即硬阻断，避免烧 GPU 前才发现问题。
- `build` 生成工作流到 `workflows/`。
- `submit` 提交工作流到本地 ComfyUI（默认 dryrun）。
- `video` 走 `pipeline_video.py`，把分镜 → 视频工作流 → 校验素材 → 提交 → 盯守 → 落盘。

---

## 2. 前置条件

| 项 | 说明 |
|---|---|
| Python | 脚本用 `sys.executable` 调自身，任意可用 Python 3 即可 |
| ComfyUI | 已启动的本地实例，默认根 `D:\Comfy-Desktop\ComfyUI-Shared`，端口 `8188` |
| 节点 | `custom_nodes/ComfyUI_MiniMaxH3_Director`（含示例工作流模板） |
| 共享输出池 | `D:\Comfy-Desktop\ComfyUI-Shared\output`（素材与成片都落这里） |

> 若 ComfyUI 根不是默认路径，用环境变量 `COMFY_ROOT` 或 `--comfy-root` 指定。

---

## 3. 目录速览

```
剧本/                    数据层：00_小说原文 01_剧本 02_分镜 03_角色场景 04_音色
workflows/               build 生成的工作流 JSON
scripts/
  pipeline.py            9 步全链编排（入口）
  pipeline_video.py      视频链（build→校验→提交→盯守）
  submit_workflow.py     提交/盯守/落盘
  qc_storyboard.py       分镜 P0/P1/P2 质量门禁（出片前把关）
  episode_status.py      整集成片收口清单 + 缺失镜续跑命令
  assemble_episode.py    整集成片组装器（concat 成片 + SRT 对白字幕 + loudnorm 电平规范）
  build_h3_prompts.py    H3 官方六段式导演提示词生成器（输出 六段式.{json,md}）
  build_new_workflows.py 图片层工作流（01 角色三视图 / 03 场景 / 05 首帧 / 06 尾帧）
  build_fl2va_refs.py    视频工作流（FL2VA 首尾帧）
  build_r2v_refs.py      视频工作流（Ref2VA 参考图）
  build_video_refs.py    视频工作流（I2V）
  drama_tools.py         数据工具库
  _start_comfyui.bat     一键启动 ComfyUI
output/                  运行时生成：60_图片素材子目录 / 视频/<集>/<mode>/
```

---

## 4. 启动 ComfyUI

**方式 A（推荐）：一键脚本**

```bat
scripts\_start_comfyui.bat
```

幂等启动，端口 `8188`，输出目录指向共享池，日志写：
`scripts\_comfyui.out.log` / `_comfyui.err.log`。

**方式 B：手动**

```bat
D:\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI\.venv\Scripts\python.exe ^
  D:\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI\main.py ^
  --port 8188 --output-directory "D:\Comfy-Desktop\ComfyUI-Shared\output"
```

启动后浏览器确认 `http://127.0.0.1:8188/` 可访问。

---

## 5. 准备数据层（`剧本/`）

前 5 步是读数据，缺内容只会提示、不会中断：

```bash
python scripts/pipeline.py --all --dryrun
```

- 若 `剧本/02_分镜/第1集_分镜.json` 无镜头，`build`/`submit`/`video` 将无事可做。
- 分镜/角色库若为空，需先由 Skill 或手工生成。

---

## 6. 素材从哪来（关键）

视频链 `video` 在**真正提交前会硬校验素材**是否已在 ComfyUI 的 `output` 里生成：

```
output/02_分镜/第1集/第1集_镜01_首帧_写实CG融合_00001_.png   # 首帧
output/02_分镜/第1集/第1集_镜01_尾帧_写实CG融合_00001_.png   # 尾帧（fl2va）
output/00_角色素材/<角色名>/<角色id>_三视图_写实CG融合_00001_.png  # r2v
output/01_场景素材/<场景名>/<场景名>_九宫格_00001_.png          # r2v
```

这些图由**图片层**生成：build 出工作流（`01_char3view` / `03_scene3view` /
`05_shotfirst` / `06_shotlast`），再 submit 到 ComfyUI 出图，图自动落到上面对应子目录。

> 素材缺失时：`--submit-real` 会**中断**并列出缺图；dryrun 只提示、不阻断。
> 先补齐素材再出片，是全链唯一需要人工准备的环节。

---

## 7. 一键全链

**安全预览（不消耗算力）** —— 推荐先看：

```bash
python scripts/pipeline.py --all
```

- `submit` 用 dryrun（只离线转换工作流，不真正提交）。
- `video` 只 build + 校验素材，不提交。

**真实出片**：

```bash
python scripts/pipeline.py --all --submit-real
```

- 会真正提交图片层与视频层工作流。
- 注意：第 7 步 `submit` 会把 `workflows/*.json`（含视频工作流）都提交但**不带盯守**；
  若要对视频精细控制（指定镜号 / 盯守 / 落盘），建议用下面的单步 `video`。

---

## 8. 分步 / 单步执行

```bash
# 只跑单个环节（支持别名：script/extract/storyboard/voice/grid）
python scripts/pipeline.py --step grid --dryrun
python scripts/pipeline.py --step build
python scripts/pipeline.py --step submit --submit-real

# 只跑视频层（全镜 / 指定镜）
python scripts/pipeline.py --step video --mode fl2va --submit-real
python scripts/pipeline.py --step video --mode fl2va --shots 01,02 --submit-real
```

---

## 9. 视频链单独用（`pipeline_video.py`）

```bash
# 建工作流 + 校验素材（安全）
python scripts/pipeline_video.py --mode fl2va --shots 01,02

# 建 + 校验 + 真实提交 + 自动盯守到出片
python scripts/pipeline_video.py --mode fl2va --shots 01,02 --submit-real
```

产物落盘：`output/视频/第1集/fl2va/`（可用 `--collect-dir` 改打印目录）。

---

## 10. 常用参数

| 参数 | 作用 | 默认 |
|---|---|---|
| `--all` | 按序跑完 9 步（含 qc 质检） | — |
| `--qc-block` | qc 遇 P0 致命项硬阻断整链 | 关 |
| `--qc-report` | 质检报告写盘路径 | `output/第1集_分镜质检.md` |
| `--step <name>` | 只跑单步（支持别名） | — |
| `--submit-real` | 真正提交到 ComfyUI（消耗算力） | 关 |
| `--dryrun` | 等价于不传 `--submit-real`（安全） | 开 |
| `--video-mode {i2v,fl2va,r2v}` | 视频模式 | fl2va |
| `--video-shots 01,02` | 只出指定镜（视频层） | 全部 |
| `--no-turbo` | 视频不插 turbo LoRA、保持 25 步（i2v/fl2va） | 关 |
| `--no-watch` | 提交后不自动盯守 | 关 |
| `--poll N` / `--timeout N` | 盯守轮询间隔 / 超时秒 | 10 / 3600 |
| `--episode 第N集` | 集号（pipeline_video.py） | 第1集 |
| `--comfy-root <path>` | ComfyUI 根（pipeline_video.py） | 默认共享池 |
| `--collect-dir <path>` | 盯守成功产物落盘目录 | output/视频/<集>/<mode> |

---

## 11. 产物落盘位置

| 产物 | 位置 |
|---|---|
| 角色三视图工作流 | `workflows/01_char3view_*.json` |
| 场景九宫格工作流 | `workflows/03_scene3view_*.json` |
| 首帧 / 尾帧工作流 | `workflows/05_shotfirst_*.json`、`06_shotlast_*.json` |
| 视频工作流 | `workflows/09_video_FL2VA_*.json`、`10_video_Ref2VA_*.json` |
| 视频成片 | `output/视频/<集>/<mode>/` |

---

## 12. 出片后处理（六段式导演稿 / 整集成片）

**H3 六段式导演提示词**（确定性生成，供导演审读 / 喂 H3 Director / 归档）：

```bash
python scripts/build_h3_prompts.py                 # 默认第1集
```

产物：`output/第1集_H3导演提示词_六段式.{json,md}` —— 按官方六段结构
（subject_definition / summary / retention_analysis / detailed_description /
overall_soundscape / non_diegetic_music），复用无缝链音色锚 S1~S11 与
`<d>[Chinese]…</d>` 台词方言，全确定性、零模型、零算力。

**整集成片组装**（把已成片分镜按 shot_id 时序 concat，可烧对白字幕 + loudnorm）：

```bash
python scripts/episode_status.py                                              # 先看整集收口清单
python scripts/assemble_episode.py --episode 第1集 --mode r2v --shots 01,02 --dryrun    # 只出组装计划
python scripts/assemble_episode.py --episode 第1集 --mode r2v --shots 01,02,03,04       # 真实拼装
python scripts/assemble_episode.py --no-sub --no-loud                          # 只拼接，不做字幕/电平
python scripts/assemble_episode.py --out <path>.mp4 --sub-out <path>.srt       # 自定义落盘
```

- ffmpeg 自动探测（imageio-ffmpeg 自带 / 系统），无需 ffprobe。
- 无对白空镜不产生字幕条目；缺镜按收口清单跳过并提示。
- 码流不一致自动回退 libx264 重编码；烧字幕后再做 loudnorm（-14 LUFS）。
- 产物：`output/视频/<集>/<mode>/<集>_<mode>_整集成片.mp4` + 同名 `.srt`。

---

## 13. 常见问题

**素材缺失，`video` 中断**
缺首帧/尾帧/三视图/九宫格。先跑图片层生成这些图，或确认已在 `output` 对应子目录。

**提示找不到模板**
`--comfy-root` 指向不包含 `custom_nodes/ComfyUI_MiniMaxH3_Director` 的目录。用 `--comfy-root` 指到正确根。

**提交报错 / 显存不足**
视频用 `--no-turbo`（不插 turbo LoRA、25 步）降低资源。图片层同理降规格。

**ComfyUI 连不上**
确认 `8188` 端口可访问，或用 `scripts/_start_comfyui.bat` 启动后重试。

**`--all` 没跑 video**
前面某一步失败会立刻停下（失败即停）。看报错提示的处理建议修一下再跑。
