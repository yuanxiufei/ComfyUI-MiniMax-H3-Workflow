# MiniMax H3 加速方案落地说明（A5000 24G + 96G 内存）

> 更新时间：2026-09-09 ｜ 适用项目：D:\code\voide\ComfyUI-MiniMax-H3-Workflow
> 本文档记录本机已落地的全部加速改动、验证结果与使用规范。**全部方案均为本地部署，不走任何 API。**

---

## 一、最终加速路线（一句话）

**量化 int8 权重 + Turbo LoRA 压到 8 步（强度 0.75）+ SageAttention + TeaCache（Multishot 链）+ 480P 出片再本地超分**

四条提速链路对应收益（社区实测/估算，见文末口径）：
- Turbo LoRA 8 步：约 3 倍（25 步 → 8 步）
- SageAttention：约 1.7~1.9 倍（同任务 603s → 324s，12G 卡实测）
- 480P 出片（1280×736 → 864×480）：像素降 57%，每步耗时约减半
- TeaCache：约 -14%~-21%（有轻微画质漂移风险，先对比再批量）

---

## 二、硬件与资产清单（已就位）

| 类别 | 内容 | 状态 |
|---|---|---|
| 显卡 | NVIDIA RTX A5000 24GB（Ampere sm86） | ✅ |
| 内存 | 96GB 系统内存（全套权重约 42.5G 可常驻内存，几乎不碰硬盘） | ✅ |
| ComfyUI | v0.34.2（≥0.30 要求满足），Python 3.13，torch 2.12.1+cu130 | ✅ |
| 扩散模型 | `minimax_h3_fl2va_pruned_int8_convrot.safetensors` / `minimax_h3_ref2va_pruned_int8_convrot.safetensors` | ✅ |
| 文本编码器 | `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors`（8G 卡也要求此版） | ✅ |
| 双 VAE | `minimax_h3_video_vae_fp16` + `minimax_h3_audio_vae_fp32` | ✅ |
| Turbo LoRA | fl2v 8step v1.0（主力）、ref2v 8step v1.0 768p、ref2v 4step v0.1（备用） | ✅ |
| SageAttention | pip 包 1.0.6 已装，启动参数已加 | ✅ |
| TeaCache | `custom_nodes/ComfyUI-MiniMaxH3-TeaCache`（与本地下载逐字节一致） | ✅ |
| Multishot / Motion-Context | 两个节点包已恢复（去掉 .disabled） | ✅ |
| 超分模型 | `4x-UltraSharp.pth`（66.9MB）+ `RealESRGAN_x4plus.pth`（67MB）→ `models/upscale_models/` | ✅ |

---

## 三、已执行改动清单（文件级）

### 1. 启动脚本 —— SageAttention 全局生效
文件：`scripts/_start_comfyui.bat`
- 启动参数新增 `--use-sage-attention`（ComfyUI 0.34.2 内置，无需任何自定义节点）
- 全部工作流（Director 链 + Multishot 链）自动吃该加速

### 2. 节点包恢复 —— 连续剧能力回归
`D:\Comfy-Desktop\ComfyUI-Shared\custom_nodes\` 下：
- `ComfyUI-H3-Multishot.disabled` → `ComfyUI-H3-Multishot`
- `ComfyUI-H3-Motion-Context.disabled` → `ComfyUI-H3-Motion-Context`

> Multishot 是唯一支持多镜无缝链式成片（无切痕、音频不断）的工作流族；Motion-Context 是 `context_pin` 无缝衔接模式的前置依赖，必须一起恢复。

### 3. TeaCache 节点包安装
- 已安装至 `D:\Comfy-Desktop\ComfyUI-Shared\custom_nodes\ComfyUI-MiniMaxH3-TeaCache`
- 类名 `MiniMaxH3TeaCache`（MODEL 级 patch，`set_model_unet_function_wrapper`），可插在任意采样器之前
- 参数：`rel_l1_thresh=0.15`、`start_step=2`、`end_step=-2`、`total_steps=8`（**必须与采样步数一致**）

### 4. R2V 工作流补加速（原为 25 步无 LoRA）
文件：`scripts/build_r2v_refs.py`
- 新增常量：`TURBO_LORA_R2V`（ref2v 8step v1.0 768p）、`TURBO_STEPS = 8`、`TURBO_STRENGTH = 0.75`
- `build_r2v()` 增加 `use_turbo=True` 参数：UNET → `LoraLoaderModelOnly`(0.75) → Director，替代原来的 UNET 直连 Director
- `rebuild_r2v_timeline()` 增加 `steps=None` 参数，写入 Director 控件 `wv[13] = 8`
- 已用 `--shots 02` 重新生成 `workflows/14_video_R2VA_镜02_1280x736.json` 并验证：steps=8、LoRA=ref2v 8step @0.75

### 5. Multishot 工作流三件套（11/12/13）优化
`workflows/` 下 11（CORE）、12（Extend）、13（剧本）：
- `H3LoraStack` 第一个槽位挂 `minimax_h3_fl2v_turbo_8step_v1.0_comfyui_bf16.safetensors`，强度 0.75
- `H3StudioControls` 与采样器 steps 14 → 8
- 在 LoraStack → 采样器之间插入 `MiniMaxH3TeaCache` 节点（`[0.15, 2, -2, 8]`，total_steps=8）
- 12 的 `H3SpeedBoosters` 保持 teacache=OFF（避免参数不匹配，手动节点更可控）
- 已验证：JSON 可解析、无悬挂链接、TeaCache 节点存在、steps=8、LoRA 已挂

### 6. FL2VA 工作流 LoRA 强度统一
- `scripts/build_fl2va_refs.py`：新增 `TURBO_STRENGTH = 0.75`，LoRA 插入改用该常量（原 1.0）
- `workflows/09_video_FL2VA_1280x736.json`、`09_video_FL2VA_整集_1280x736.json`、`09_video_FL2VA_镜01_1280x736.json`：LoRA 强度 1.0 → 0.75

> 社区实测：LoRA 强度拉满 1.0 画面可能不跟提示词，0.75 左右为甜点。

### 7. 超分模型就位
`D:\Comfy-Desktop\ComfyUI-Shared\models\upscale_models\`：
- `4x-UltraSharp.pth`（66,961,958 字节，推荐）
- `RealESRGAN_x4plus.pth`（67,040,989 字节，备选）

---

## 四、各工作流加速配置对照表

| 工作流 | 权重 | Turbo LoRA | 步数 | 采样器/调度器 | 缓存加速 | 状态 |
|---|---|---|---|---|---|---|
| 09 FL2VA（首尾帧） | fl2va pruned int8 | fl2v 8step v1.0 @0.75 | 8 | res_multistep / simple | 不适用（Director 链不兼容） | ✅ 已优化 |
| 14 R2V（角色参考） | ref2va pruned int8 | ref2v 8step 768p @0.75 | 8 | res_multistep / simple | 不适用 | ✅ 已优化 |
| 11 Multishot CORE | fl2va pruned int8 | fl2v 8step v1.0 @0.75 | 8 | euler / beta | TeaCache (0.15,2,-2,8) | ✅ 已优化 |
| 12 Multishot Extend | fl2va pruned int8 | fl2v 8step v1.0 @0.75 | 8 | euler / beta57 | TeaCache (0.15,2,-2,8) | ✅ 已优化 |
| 13 Multishot 剧本 | fl2va pruned int8 | fl2v 8step v1.0 @0.75 | 8 | euler / beta | TeaCache (0.15,2,-2,8) | ✅ 已优化 |

> 说明：Director 链（09/14）的采样由 Director 内部 comfy 官方节点链完成，采样器级缓存（TeaCache/TE-Speed）接不进去；缓存加速只在 Multishot 链可用。SageAttention 为全局参数，两条链都生效。

---

## 五、重启与验证

改动全部完成后**必须重启 ComfyUI**（当前 9:25 启动的进程不带新参数、未加载新节点）：

```powershell
# 1. 停止当前 ComfyUI（只杀 main.py 进程）
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Where-Object { $_.CommandLine -like '*main.py*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }

# 2. 用新参数启动（SageAttention + 新节点已就绪）
& "D:\code\voide\ComfyUI-MiniMax-H3-Workflow\scripts\_start_comfyui.bat"
```

重启后验证三点：
1. 节点列表能搜到 **MiniMax H3 TeaCache**（新节点加载成功）
2. 打开 11/12/13 工作流不再报红（Multishot 节点恢复）
3. 控制台无 sage 报错，跑一条镜对比耗时

---

## 六、使用建议

1. **出片规格**：优先 864×480 出片 → `4x-UltraSharp` 本地超分到 1080P。直接跑高分辨率耗时成倍增长。
2. **画质上限**：本地开源的是 H3-Base，输出上限 768P；2K 只能走官方 API（约 0.8 元/秒）——本项目坚持全本地，用超分模型补分辨率。
3. **长镜拆分**：单镜 12s 建议拆 2×6s（R2V 已开 22 帧 `continuityOverlapFrames` 衔接）；整集（4608/2136 帧）用 `--shots` 按镜提交，降低失败重跑成本。
4. **LoRA 强度**：保持 0.75，勿拉满 1.0。
5. **缓存加速**：TeaCache/Spectrum/EasyCache 可能轻微画质下降、人物畸变，且不可互相叠加；先开一条对比再批量。`blockcache` 在 ≤14 步采样下实测 0 命中，**不要开**。
6. **节点切换**：Turbo LoRA 与原生 LoRA 节点之间切换必须先重启 ComfyUI（显存不自动释放，直接切必爆显存）。
7. **音频**：H3 中文口播仍不稳定，重要对白建议单独 TTS 后期配音。

---

## 七、实测数据与口径

| 数据 | 数值 | 来源/口径 |
|---|---|---|
| SageAttention 收益 | 同任务 603s → 324s（≈1.86x） | 12G 卡（4070/4070S）社区实测 |
| 缓存类收益 | TeaCache@0.30 -21%、Spectrum -29%、EasyCache ≈-21% | Multishot 包内 benchmark（640×1152/192f/14步） |
| TE-Speed | 叠加在 Sage+Turbo 之上仅省 ~11s，边际收益非刚需 | 实测文章 |
| 24G 卡（4090） | 15s/768P 开加速约 4 分钟，不开约 8 分钟 | 社区实测（A5000 可参照） |
| A5000 + 96G | 预计 2~4 分钟 / 5s·480P | **估算**（按卡型推算，需实跑验证） |
| LoRA 强度 | 0.75 左右为甜点，1.0 可能不跟提示词 | 实测文章 |
| 本地上限 | 768P（Context-IR / Regenerate-2K 未开源） | 官方公告 |

主要参考：什么值得买《MiniMax H3本地部署实测汇总》（2026-08-12）、ComfyUI-H3-Multishot 包内实测注释、ComfyUI 官方文档。

---

## 八、坑位清单（社区实测）

1. ComfyUI 必须 ≥0.30.0（本机 0.34.2 ✅）
2. Turbo 与原生 LoRA 切换必须重启 ComfyUI
3. 小显存卡文本编码器必须用 nvfp4_awq 版
4. 显存 <24G 别碰 BF16 完整版（单变体全量约 130GB）
5. 中文口播不稳定，建议 TTS 后期配音
6. 许可证为社区许可，美国/欧盟/英国/韩国需单独授权，出海前必读条款
7. 官方稀疏注意力未开源，现全注意力推理非完全体，仍有优化空间

---

## 附：常用命令

```powershell
# 生成 R2V 单镜工作流（自动复制参考图到 input）
python scripts/build_r2v_refs.py --shots 02 --comfy-input "D:\Comfy-Desktop\ComfyUI-Shared"

# 生成 R2V 整集工作流
python scripts/build_r2v_refs.py --comfy-input "D:\Comfy-Desktop\ComfyUI-Shared"

# 启动 ComfyUI（含 --use-sage-attention）
scripts\_start_comfyui.bat
```
